// Si vamos a la consola podemos utilizar el simbolo  de dolar $ para acceder al historial de los elementos que seleccionamos 

$0

// Por ejemplo si presiono 0 este fue el ultimo elemento que seleccione.

$1

// Y si selecciono uno este es el penultimo y presiono Enter. 

$1. 

// Ahora presionamos punto para acceder a las propiedades y a los metodos de este objeto, podemos ver muchisimas propiedades y muchisimos metodos 

// Podemos ver mas detalles de donde vienen esas propiedades y metodos:

// HTMLImageElement
// HTMLElement
// Element
// Node
// EventTarget
// Object

document.

// Aqui podemos ver una URL 

document.body

// Aqui ningun otro elemento va a tener body

document.body. 

// Ahora si presiono punto puedo acceder a propiedades como este objeto llamado HTMLBodyElement

document.body.bgColor = 'black'

// Hemos logrado cambiar esta propiedad y eso sera reflejado directamente en el navegador 

$0.src

// Si presiono src puedo ver la direccion exacta de donde vino esa imagen 

$0.getElementsByTagName('p')

// Buscamos los tags con el elemento 'p' y asi ya tenemos un buen entendimiento de como funciona cada elemento de HTML 